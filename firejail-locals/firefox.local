# Workaround allowing Nightly to execute from ~/.
ignore noexec ${HOME}

# Sometimes I do need to access stuff in Documents.
noblacklist ${DOCUMENTS}
whitelist ${DOCUMENTS}
read-only ${DOCUMENTS}

# I _do_ want u2f.
ignore nou2f

# I _do_ want to open links the easy way.
ignore nodbus

# https://github.com/netblue30/firejail/discussions/4860
ignore private-dev
